def count_words(text):
    """Returns the number of words in the input text"""
    return len(text.split())

def count_characters(text):
    """Returns the number of characters in the input text"""
    return len(text)


"Add my_module.py with two functions"
